server :
  port : 8761 # 当前eureka server的服务端口
eureka:
  client:
    register-with-eureka: false #不允许注册eureka自己，不把自己当做微服务注册
    fetch-registry: false # 不同步其他注册中心的数据
    service-url:
      defaultZone : http://localhost:8761/eureka # 默认注册中心的访问地址
